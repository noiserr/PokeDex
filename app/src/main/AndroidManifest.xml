<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="third.task.dod.pokedex" >

    <application
        android:name=".PokedexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Pokemon" >
        <meta-data
            android:name="AA_MODELS"
            android:value="third.task.dod.pokedex.entity.Pokemon,                         third.task.dod.pokedex.entity.CouchPokemons,                          third.task.dod.pokedex.entity.Couch" />
        <meta-data
            android:name="AA_DB_NAME"
            android:value="pokemon.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="7" />

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.DetailsActivity"
            android:label="@string/title_activity_details" >
        </activity>
    </application>

</manifest>
